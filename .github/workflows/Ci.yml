name: 'CI Workflow'

on:
  pull_request:
    branches:
      - main
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    env: 
      FOO: Foo value
      BAR: Baz value 

    steps:
      - name: Clone repo
        uses: actions/checkout@v4

      - name : Linter 
        uses: astral-sh/ruff-action@v3